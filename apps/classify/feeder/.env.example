# Motia Workflow API
MOTIA_API_URL=http://localhost:3000
# For Railway deployment:
# MOTIA_API_URL=https://classify-workflow.up.railway.app

# LibSQL / Turso Database (source indicators)
LIBSQL_URL=libsql://your-database.turso.io
LIBSQL_AUTH_TOKEN=your_auth_token_here
# For local LibSQL:
# LIBSQL_URL=file:./local.db

# Postgres Database (results tracking)
POSTGRES_URL=postgresql://user:password@host:5432/database
# For Railway:
# POSTGRES_URL=${{Postgres.DATABASE_URL}}

# Batch Configuration
BATCH_SIZE=10           # Indicators per batch
CONCURRENCY=5           # Concurrent batches (50 indicators at once)
INTER_BATCH_DELAY_MS=300  # Delay between batch groups
PROVIDER=openai         # openai | anthropic | local

# Rate Limits Reference (OpenAI GPT-4o-mini Tier 1)
# TPM: 200,000 tokens/minute
# Per indicator: ~1,000 tokens (2-3 LLM calls)
#
# Recommended configs:
# - BATCH_SIZE=10, CONCURRENCY=5 → 50 concurrent, 75% TPM (RECOMMENDED)
# - BATCH_SIZE=10, CONCURRENCY=3 → 30 concurrent, 45% TPM (conservative)
# - BATCH_SIZE=10, CONCURRENCY=8 → 80 concurrent, 100% TPM (max Tier 1)
