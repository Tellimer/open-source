/**
 * Custom units parser for V2 (copied from ../../../custom/custom_units.ts)
 *
 * This file contains custom unit parsing functions needed by V2 workflows.
 * All dependencies have been localized to make V2 independent.
 */

// Re-export the main functions that V2 needs
export { parseWithCustomUnits } from "../../custom/custom_units.ts";
