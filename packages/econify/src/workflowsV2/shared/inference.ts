/**
 * Inference utilities for V2 (copied from ../../../inference/inference.ts)
 *
 * This file contains unit inference functions needed by V2 workflows.
 * All dependencies have been localized to make V2 independent.
 */

// Re-export the main functions that V2 needs
export { inferUnit } from "../../inference/inference.ts";
